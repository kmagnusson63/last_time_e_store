<div id="content">
  <h2>Search Results</h2>
  <% if @products.present? %>
  <% elsif @products.empty? %>
    <p>'<%= @keywords %>' returned no results</p>
  <% end %>
  <%= render @products %>
</div>