<%= form_tag(add_to_shopping_cart_path(product), method: "post") do %>
 
  <%= select_tag('quant',options_for_select(1..10)) %>
  <%= submit_tag('Add to Cart') %>
<% end %>
<%= form_tag(single_checkout_path(product), method: "post") do %>
	<% submit_tag('Buy NOW!') %>
<% end %>